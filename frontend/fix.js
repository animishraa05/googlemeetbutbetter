const fs = require('fs'); let c = fs.readFileSync('src/app/pages/TeacherRoomPage.tsx', 'utf8'); c = c.replace('onClick={() => setIsDrawing((p) => !p)}', 'onClick={() => { setIsDrawing((p) => { const n = !p; if (!n) { elementsRef.current = []; if (excalidrawAPI) { excalidrawAPI.updateScene({ elements: [] }); } if (localParticipant) { localParticipant.publishData(new TextEncoder().encode(JSON.stringify({ type: ''clear_whiteboard'', timestamp: Date.now() })), { topic: ''proxima_events'' }).catch(console.error); } } return n; }); }}'); fs.writeFileSync('src/app/pages/TeacherRoomPage.tsx', c);
